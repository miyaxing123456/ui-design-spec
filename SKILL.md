---
name: ui-design-spec
description: Turn a UI design mockup/截图/设计稿 into a precise, implementable frontend specification. Use this skill when the user提供了设计稿/图片/UI 图, asks to 分析界面、生成前端规范、把设计转成可落地代码, or needs a structured report covering layout (flex/grid), responsive strategy (rem vs vw/vh vs px vs clamp), forms, tables, design tokens (colors/typography/spacing/radius/shadow), component breakdown, scroll behavior, and text-overflow handling. Works for both 后台/中后台 (admin, dashboard, Ant Design style) and C端 (marketing, landing, feed) interfaces. Core style is question-driven: keep asking the user targeted questions to converge on what the report should emphasize, instead of dumping a fixed template. Use this skill when the user wants the analysis direction to be steered by their answers.
license: Complete terms in LICENSE.txt
---

# UI Design Spec — 设计稿 → 可实施前端规范（提问驱动）

把一张设计稿（截图/图片/蓝湖/Figma 导出）转成**前端能直接照着写代码的规范**。核心思路：不是"好看就行"，而是把每一项落到**可量化、可复用的 token 和决策**上。

**适用场景**：用户给 UI 图/设计稿，要求分析、生成前端规范、复刻界面、拆组件、定响应式方案。**不适用**：纯创意构思、无需落地的脑暴。

> **本技能的核心工作方式：提问驱动（Question-Driven）。**
> 不一次性输出所有板块的模板，而是**先通过多轮提问**搞清楚用户的重点、偏好、工程环境，然后**只针对用户关心的部分给出深挖结论**，其余记为「略过/默认」。每一步都让用户来定方向，而不是替用户假设。

---

## 工作流总览（5 步：问 → 判 → 深挖 → 补问 → 定稿）

```
[1] 冷启动提问   —— 一次性抛出一组基础问题（类型/响应式[A]/宽高[B]/视口[C]/目标/重点/环境）
[2] 类型判定      —— 从答案映射到「决策矩阵」，得到默认倾向
[3] 定向深挖      —— 只对用户圈定的板块逐项输出（其余「略过」）
[4] 迭代补问      —— 对不确定/多变的点，边输出边补问（一次 1-3 个）
[5] 定稿          —— 汇总成「决策清单 + Token 表 + 结构图 + 组件树」+ 质量自检 + 待确认项（可选落盘产物）
```

**关键原则**：
- **一次只问 1–3 个问题**，回答后立刻推进；不要一次抛 10 个问题把用户淹没。
- **问题要有选项**（给出默认推荐项），别问开放式大而全的问题。
- **用户的回答决定输出方向**，不是反过来让输出决定一切。宁可少覆盖、也要覆盖对。
- **不确定的/设计稿看不到的**（校验态、数据规模、断点、组件库）→ 通过补问让用户定，而不是我自己拍。

---

## 第 1 步：冷启动提问（先抛这组，收敛大方向）

一上来，先问这一组（可合并成 1–2 条消息，每条带推荐选项）。**其中 [A] 响应式策略 和 [B] 最小/最大宽高 是两条独立轴，必须优先单独问清**——它们直接决定整份规范走向，不能从「界面类型」间接假设。

> ⚠️ **重要**：**不要根据界面类型默认替用户选响应式方案**。市面上"是否用 vw/vh"是**场景化选型**（C端营销/活动页、大屏看板、移动 H5 普遍用 vw/vh 整页流式缩放；后台/中后台普遍用固定宽高+弹性布局 px/rem）。到底用哪种，**必须让用户自己选**。

1. **界面类型**：后台/中后台 │ C端营销/落地页 │ 信息流/App
   - 作为背景参考，但**不直接决定响应式方案**。同时影响动效尺度。
2. **[A] 响应式策略**（重点，独立轴）：
   - **整页流式响应式（vw/vh / rem 等比缩放）** —— 适合"宽多少都跟设计稿一模一样"（C端营销、活动页、大屏看板、移动 H5）。
   - **固定宽高 + 弹性布局（px/rem + flex/grid）** —— 适合"铺满可用宽度、数据密集"（后台/中后台）。
   - **混合** —— 主体弹性布局，个别全屏区块（hero）用 vw/vh。
   - **决定**单位选型（vw/vh vs px/rem）和所有断点逻辑。
3. **[B] 是否设置最小/最大宽高**（重点，独立轴）：
   - **只设最小**（防过窄塌陷） │ **只设最大**（防 4K 拉长） │ **都设** │ **都不设**（纯自适应/纯流式）。
   - **决定**是否需要 `min-width`（侧栏/每列/页面）和 `max-width`（内容区超宽居中）。
4. **交付目标**：只出分析报告 │ 报告+可直接开写的代码骨架 │ 报告+**落盘产物文件** │ 只要组件拆分/某模块
   - 决定输出深度（是否出 token 表/结构图，还是只盯某一块）。
   - **「落盘产物」**：把 Token/规范导出成**项目内可复用的文件**（`tokens.css` / `tokens.scss` / `design-tokens.json` / 一份 `DESIGN.md`），而不只是会话里的一段文本。**强推**——这是让 skill 真正"可用"的关键（对齐市场上产出可编辑规范的做法）。
5. **[C] 视口范围**（重点，新增）：
   - **只有桌面稿** │ **桌面 + 平板 + 移动（多视口）** │ 有其他尺寸（我补充）。
   - 多视口时，除了分析主稿，还要给出**不同断点下的布局变化建议**（如卡片 3列→2列→1列、侧栏折叠成图标栏、文案换行/省略变化）。
6. **你最关心哪几块**（可多选）：
   - 布局(flex/grid) │ 响应式单位 │ 表单 │ 表格 │ 卡片/组件拆分 │ 滚动/超长处理 │ 配色/token │ 其他
   - **这就是「定向深挖」的圈定范围**，只对这些深挖，其余略过。
6. **工程环境**：UI 库(AntD?/Element?/自定义) │ 框架(Vue3/React/原生) │ 目标最小宽度 │ 是否需随屏放大文字
   - 决定 token 注入方式、断点、组件拆分方式、单位选型。

> 如果用户不想答全部，至少确认 **[A] 响应式策略 + [B] 最小/最大宽高 + [C] 视口范围 + 最关心的板块** 这四个，其余用默认。

---

## 第 2 步：类型判定（把回答映射到默认倾向）

### 决策矩阵 A：界面类型 → 默认倾向（**仅背景参考，响应式方案以用户 [A]/[B] 回答为准**）
| 类型 | 布局/单位倾向（默认） | 动效尺度 | 是否用 vw/vh |
|---|---|---|---|
| **后台/中后台** | flex/grid 自适应 + px/rem | 克制 | 默认不用（除非用户选流式） |
| **C端营销/落地页** | vw/vh、clamp() 流式，可全屏 hero | 可大胆 stagger/滚动触发 | 常见 |
| **信息流/App** | rem + 弹性，长列表虚拟滚动 | 适度 | 移动 H5 常见 |

> **关键**：表格里只是"倾向"。真正决定单位选型的是 **冷启动 [A] 响应式策略**；真正决定是否设 min/max 的是 **冷启动 [B]**。**不要**看到"后台"就默认 px/rem——如果用户明确要整页流式缩放，后台也能用 vw/vh。**一切以用户 [A]/[B] 回答为最终依据。**

### 决策矩阵 B：工程环境 → 影响
| 因素 | 影响 |
|---|---|
| UI 库 | 决定设计稿**映射到现成组件**还是定制；token 注入方式（CSS Variables/SCSS/Tailwind） |
| 框架 | 决定组件拆分、slot/composition 方式 |
| 目标最小宽度 | 决定折叠断点（后台常见 1280，低于侧栏折叠成 64px 图标栏） |
| 是否随屏放大文字 | 决定 rem 还是 clamp() |

**输出**：用一两句话给出「类型判定 + 默认倾向」，作为后面深挖的基线，并**请用户确认或纠正**。

---

## 第 3 步：定向深挖（只对用户圈定的板块输出）

对上面第 1 步「最关心的板块」**逐个**输出，每个板块遵循 **结论 + 依据 + 实现建议 + (可选)示例值** 四段式。**未圈定的板块**，在报告里标注「略过（本次不关注）」即可，不展开。

下方是**各板块的完整检查清单**（供深挖时对照，不是每次都全输出）。

### ① 布局与栅格 Layout
- **Flex vs Grid 判断**：Flex=一维（单轴、需 grow/shrink、按内容自适应）→ 顶栏/按钮组/导航/表单行/卡片堆叠；Grid=二维（同时控行列、显式列轨道、跨行列、对齐共享网格）→ 页面骨架/多列工作区/卡片矩阵。**通用原则：壳层/单轴用 Flex，多列独立区用 Grid。**
- 栅格：列数、gutter、container 宽/`max-width`、断点。
- 间距节奏 `--space 4/8/12/16/24/32`；对齐（space-between/基线/居中）。
- **多视口/断点（按用户 [C] 回答定向）**：
  - 若用户有移动/平板稿，或要求覆盖多视口 → 给出**各断点下的布局变化**：卡片 3列→2列→1列（`repeat(auto-fill,minmax())` 自动收窄）、侧栏折叠成图标栏（`64px`）、顶栏 tabs 横向滚动、文字随宽度换行/省略等。
  - 若只有桌面稿 → 至少说明"低于此宽度会发生什么"（侧栏折叠/列数下降），不必展开移动端。
  - 常见断点参考：`sm 640 / md 768 / lg 1024 / xl 1280`（后台常从 1280 起，低于折叠）。

### ② 响应式单位决策（重点，按用户 [A] 回答定向）
**原则：单位分「用途」选，不整页统一。**
| 单位 | 特性 | 适合 | 不适合 |
|---|---|---|---|
| px | 绝对 | 1px 细线、小图标、头像、圆角 | 长文本、需随用户缩放 |
| rem | 基于根字号(16px) | **文字/字号/间距/图标尺寸** | 宽窄自适应 |
| vw/vh | 基于视口 | 全屏 hero、宽高比容器、整页流式 | ⚠️ 密集文字/表格（缩放破坏可读性） |
| % / flex·grid | 基于父容器 | 布局宽窄自适应 | 需精确值 |
| clamp() | clamp(min,流式,max) | 流式字号/宽高、平滑过渡 | 复杂多断点 |

**按用户 [A] 响应式策略定向输出**：
- **用户选「整页流式(vw/vh)」** → 布局/尺寸用 vw/vh（或 rem 等比，root=设计宽/10）；但**正文文字仍建议 rem/clamp 兜底**，避免过宽/过窄时文字忽大忽小。
- **用户选「固定宽高+弹性」** → 布局 flex/grid + px/rem，**不用 vw 做文字**；文字/间距 rem，细线/图标 px。
- **用户选「混合」** → 主体按固定+弹性，全屏 hero / banner 区块用 vw/vh + clamp。

**必须落地的结论**：结合用户「是否随屏放大文字」，明确回答"用 rem 还是 clamp()"，不要含糊。

### ③ 是否设置 min/max 宽高（按用户 [B] 回答定向）
按用户在冷启动 [B] 的选择输出，不做默认假设：
- **只设最小** → 给 `min-width`：侧栏、每列（minmax 下限）、页面整体（跌破进入折叠/收缩）。
- **只设最大** → 给 `max-width`：超宽屏内容区 `max-width:1600px` 居中防拉长。
- **都设** → 以上两组都给，容器用 `minmax()` / `clamp()` 表达区间。
- **都不设** → 纯自适应或纯流式，不给硬性 min/max（此时改用 `clamp()` 保证不塌陷/不拉长）。
- **高度**：后台 `100vh`；营销页可随内容流。**结合 [B] 是否固定高度一起给。**

> ⚠️ 不要因为"它是后台"就默认必设 min/max——以用户 [B] 的回答为准。

### ④ 固定 vs 滚动区域
- 逐区域标注：**固定宽高**（顶栏、侧栏宽、列头、吸顶工具条、底部用户卡）vs **滚动区**（列表体、菜单超高、内容区）。
- 列头/筛选条通常吸顶固定，下方列表体 `overflow-y:auto`。

### ⑤ 滚动策略：容器内滚动 vs 整页延伸 — ⚠️ **元素可能多时必须询问用户，并告知影响**
> **判断某项元素（列表/卡片/菜单/表格行/消息）数量可能变多时，必须把「滚动策略」作为必问项抛给用户，并**说明每种选择的后果**，让用户在知情下决策。** 这不是"给个默认方案"，而是"告知影响+给建议"。

**⚠️ 必问项（当元素数量可能多时）**：
1. **滚动归属**：这大片内容变多后，是**容器内滚动**（该区域自己滚），还是**随内容延伸**（整页跟着变长滚动）？
2. **要明确告知的影响（关键）**，逐条说清：
   - **是否会引发整页滚动**：容器内滚动 → 页面主体高度固定（`100vh`），整页不滚；随内容延伸 → 页面 body 会被内容撑高，**整页滚动条出现**。
   - **是否会出现多个滚动条**：若页面主体固定 + 某区域容器内滚动 → 可能出现 **1 个外层整页滚动 + N 个内层区域滚动**，即"**多滚动条**"；用户需确认这是否符合预期。
   - **长内容性能**：内容很多时，容器内滚动可配合**虚拟滚动 / content-visibility:auto**；随内容延伸则需考虑首屏渲染与懒加载。
   - **吸顶/固定区**：容器内滚动能保持列头/工具条固定；整页延伸则 header 需 `sticky`。

**常用建议（供参考，但以用户回答为准）**：
- 后台多列/数据列表 → **容器内独立滚动**（`html/body 100vh`，各列 `min-height:0; overflow-y:auto`），列头/筛选固定，整页不滚。**但要提前告知**：可能出现"外层一个垂直滚动条 + 每个区域一个滚动条"的多滚动条形态，若用户不想这样，改用"整页延伸 + sticky header"。
- 纯流式内容页/文章/营销长页 → **整页随内容延伸**（header 用 sticky），整页滚动。

**⚠️ 坑**：Grid/Flex 子项默认撑高父层导致内部滚不动 → 滚动容器必须 `min-height:0`，外层固定 `100vh`。

> 关键交互提示：**"容器内滚动 vs 整页延伸"不是一次定全局，而是可以不同的区域用不同策略**（如左列表容器内滚、右侧详情随内容延伸）。每处数量可能多的元素都应单独确认，不要一刀切。

### ⑥ 组件拆分 Component Breakdown
- 拆成 **页面骨架 → 区域 → 组件 → 原子件**。
- 复用原子件（`FilterPills`/`TagBadge`/`ActionButton`/`PanelCard`）单独抽出。
- 数据驱动列表（`v-for`/`map`）与静态组件分开。
- 标出**页面级私有** vs **全局可复用**。

### ⑦ 设计 Token（全量，除颜色/字体外）
- **颜色**：主/辅/语义(成功·警告·危险·信息)/中性/背景/边框。**务必像素采样给精确 `#HEX`**，不是估值。
- **字体**：font-family（中文用系统 CJK 体）、字号层级、字重、行高。
- **圆角** `--radius-sm 4 / md 8 / lg 12`；**阴影** `--shadow-card 0 2px 8px rgba(0,0,0,.06)`；**间距** `--space 4/8/12/16/24/32`；**z-index** 层级。

### ⑧ 表单 Forms
- 字段态（default/hover/focus/active/disabled/readonly/error/success/warning）、label 位置/必填*/hint、校验（样式+触发时机）、控件类型（input/textarea/select/datepicker/checkbox/radio/switch/upload/级联）、按钮层级、布局（水平/垂直/栅格/间距/输入框高·内边距·圆角·边框）。
- **设计稿通常只画一个态** → 校验错误态等**必须补问**。

### ⑨ 表格 Tables — ⚠️ **列宽与固定列必须询问用户，并给出解决方案**
> **涉及到表格，列宽策略和是否固定列是必问项**，直接影响表头吸顶、横向滚动、操作列固定、超长省略，不能默认拍板。

**⚠️ 必问项（每张表格都要问）**：
1. **每列列宽怎么设**（需逐列或按列角色确认）：
   - **固定宽度**（`width:120px`）—— 列内容定长、需精确对齐（如状态/操作列数字）。
   - **弹性/自适应**（`flex:1` 或 `width:auto`）—— 内容变长自动分配，适合文本列。
   - **等宽分配**（`table-layout:fixed` + 均分）—— 列数少、希望整齐分布。
   - **混合**：文本列弹性、操作列固定。**这是最常见做法，可作为推荐**。
   - 关键列设 `min-width`（如名称列），防过窄。
2. **是否设置固定列**：
   - **固定列**（如操作列固定右侧 `position:sticky right:0`，首列固定左侧）—— 横向滚动时关键列不被卷走。
   - **要问固定哪一列/哪几列**（通常是操作列、序号列）。表头是否 `sticky`（吸顶）。
3. **横向超宽怎么处理**：列数多 → 容器 `overflow-x:auto` + 关键列 `min-width` + 固定列；还是压缩列宽 / 换行。**是否接受横向滚动条**要给用户。
4. **单元格超长**：文本列如何省略（单行/多行）——联动 ⑬ 必问。
5. **数据规模**：行数多少 → 决定分页 / 虚拟滚动 / 是否整表渲染。

**可选方案集（供选择）**：
| 列角色 | 推荐列宽策略 |
|---|---|
| 名称/描述（文本会变长） | 弹性 `flex:1` + `min-width`，超长省略 |
| 状态/类型/标签 | 固定宽度，短 |
| 数字/金额/时间 | 固定宽度，右对齐 |
| 操作（按钮/更多） | **固定宽度 + `sticky right:0` 固定列** |
| 序号/选择框 | 固定窄列，`sticky left:0` 固定 |

> **结论**：列宽是"固定 vs 弹性 vs 等宽"的选择，固定列是"横向滚动时保护关键列"。两者都**必须由用户定**，且要**告知**：固定列 + 横向滚动时会出现横向滚动条、列宽弹性会随内容变化、等宽会牺牲等长文本的可读性。

### ⑩ 状态机 State Machine
每个组件至少覆盖：default / hover / focus / active / disabled / loading / 空 / 错误 / 选中。设计稿常只给 1 态，前端必须补齐。

### ⑪ 素材与资源 Assets — ⚠️ **图片处理必须询问用户，不能默认**
> 图片（封面/缩略图/头像/插画）是另一个高频且好出错的点。**凡是出现图片/缩略图/头像，必须问用户图片的尺寸策略**，不替用户拍板。

**⚠️ 必问项（每类图片都要问）**：
1. **图片宽高比策略**：
   - **固定宽高**（如 96×54、卡片封面固定 288×162）—— 适合列表对齐、卡片统一、需严格对齐的缩略图。
   - **保持固定宽高比**（如 `aspect-ratio:16/9`、`4/3`、`1/1`）—— 让图片随容器宽度缩放、自动适应不同宽度，适合响应式卡片网格。
   - 若固定宽高比，**要问清楚是几比几**（16:9 / 4:3 / 1:1 / 3:2…），这是最常漏的。
2. **图片裁切方式**：
   - `object-fit: cover`（填满裁切，突出主体）/ `contain`（完整显示，可能留白）/ `fill`（拉伸，可能变形）。
   - 头像通常是 **`cover` + 圆形裁切 `border-radius:50%`**，要确认。
3. **占位 & 加载**：
   - 图片缺失/加载失败时的占位图、`background` 占位、`lazy loading`、`@2x/@3x` 资源。
4. **图片尺寸偏差怎么办**：若实际图宽高比和设定不符，是裁切(cover)还是留白(contain)？是否允许变形(fill)？

**可选方案集（供选项，不是自动选择）**：
| 场景 | 方案 |
|---|---|
| 卡片封面 | 固定宽高 或 `aspect-ratio:16/9` 随宽缩放；`object-fit:cover` |
| 小缩略图/图标 | 固定宽高，`cover` 圆形/圆角裁切 |
| 头像 | 固定方图 + `cover` + 圆形裁切；或字母/图标兜底 |

> 结论：**固定宽高 vs 保持宽高比，是两种不同的实现路径（尺寸稳定性 vs 响应式契合），必须由用户选。** 默认推荐"卡片封面用 aspect-ratio 保持比例、头像用固定+圆形裁切"，但**以用户回答为准**。

### ⑫ 可访问性 & 性能 & 国际化
- a11y：对比度、`focus-visible`、语义化+ARIA（图标按钮 `aria-label`、表格 `caption`/`th scope`）、最小可点击(~44×44)。
- 性能：长列表**虚拟滚动 / content-visibility:auto / CSS containment**、图片懒加载、动画优先 CSS。
- i18n：跨语言变长（影响省略设计预留）、数字/日期/货币格式化、RTL。

### ⑬ 文字超长处理（Text overflow）— ⚠️ **必须询问用户，不能默认给结论**
> **这是最常见、最容易踩坑的点，也是技能里最需要"问"的板块。** 本文档只给出**可选方案集**，具体用哪种**必须让用户定**。判断某处有超长风险时，**强制补问**（见下"必问项"），给出方案让用户选，而不是自己拍。

**可选处理方案集（供选项，不是自动选择）**：
| 文案类型 | 可选方案 |
|---|---|
| 标题/长句（多行） | ① 多行 line-clamp 2 行 │ ② 多行 line-clamp 1 行 │ ③ 完整换行(不截断) |
| 短字段（班名/菜单/用户名） | ① 单行省略号(nowrap+ellipsis) │ ② 自动换行 │ ③ 单行省略 + tooltip |
| 面包屑/列头/按钮 | ① nowrap 不省略(宽固定) │ ② 单行省略 + tooltip |
| 一排 tabs/pills 塞不下 | ① 横向滚动 │ ② flex-wrap 换行 │ ③ 横向滚动 + 两端渐隐 |

**⚠️ 必问项（判断出超长风险时，必须问用户，每处都要）**：
1. **处理方式**：这处文字超长时，你要**换行 / 单行省略号 / 多行截断(clamp几行) / tooltip**？给出 2–3 个候选方案让用户选。
2. **单行省略号是否需要 tooltip / title 提示**？——单行或截断后，用户看不到完整内容，**必须询问**是否加 `title` 属性或自定义 `Tooltip` 组件展示全文。这是实践里高频遗漏点，**默认建议加，但以用户回答为准**。
3. **（如涉及多行 clamp）** 截断几行？是否需要"展开/收起"交互？

> 说明：`title`（原生 tooltip）成本最低但样式不可控；自定义 `Tooltip` 组件样式可控但更重。把这两个取舍告诉用户，让其决定，不替用户选。

### ⑭ 可能超长的元素清单 + **逐项询问**
逐项列出可能超长的元素、优先级，并**对每个标星 ⭐ 的元素显式询问用户处理方式**（方案见 ⑬）。
- 目标元素（标题/描述/多行文本/列表项/菜单/面包屑/按钮/tabs/pills/计数 like"9999+"等）。
- Markdown 建议格式：`[元素] —— 超长风险等级(高/中/低) —— 待确认：单行省略? 几行clamp? 要tooltip?`。
- **这些"待确认"项，统一收进最终报告的高级「待确认问题」清单，等用户回答后回填结论。**

---

## 第 4 步：迭代补问（边输出边澄清，一次 1–3 个）

深挖过程中，凡遇到**设计稿看不出来、或会影响结论**的点，立即**补问**用户，而不是替用户假设。典型补问：
- 校验错误态样式长什么样？（表单）
- 数据量大概多少？要不要分页/虚拟滚动？（表格/长列表）
- ⚠️ **元素数量可能多** → 必须问**容器内滚动 vs 随内容延伸**，并告知影响：会不会引发整页滚动、是否出现多滚动条、长内容性能、吸顶/固定区取舍。（见 ⑤）
- ⚠️ **表格列宽/固定列** → 涉及表格时，必须问①每列列宽(固定/弹性/等宽/混合)②是否设置固定列(哪列)③横向超宽是否接受横向滚动条。（见 ⑨）
- 目标最小宽度？断点怎么定？（响应式）
- 右上角各种操作按钮的优先级（主/次/危险）？
- 空结果/加载中长什么样？
- 是否已有 UI 库/token 体系，还是从零建？
- ⚠️ **文字超长**：判断某处可能超长时，必须问①处理方式（换行/单行省略/几行clamp）②单行省略是否加 `title`/`Tooltip` 展示全文。（见 ⑬）
- ⚠️ **图片处理**：出现图片/缩略图/头像时，必须问①固定宽高还是保持宽高比(几比几)②`object-fit` 裁切方式(cover/contain/fill)③头像是否圆形裁切。（见 ⑪）

**每个补问都带推荐选项**，用户选完就继续，不阻塞整体进度。

---

## 第 5 步：定稿（汇总输出，让前端能直接开写）

收尾汇总，包含：
1. **类型判定结论** + 对应推荐。
2. **只覆盖用户圈定的板块**；未圈定的标「略过」。
3. **完整 token 表**（颜色/字体/圆角/阴影/间距/z-index，精确值）。
4. **布局结构图**（壳层+各区域+滚动边界）。
5. **组件树**（骨架→区域→组件→原子件，标出可复用项）。
6. **已确定决策清单** + **仍待确认/留白项**（后续可继续补问）。
7. **（可选）落盘产物**：若用户在「交付目标」选了落盘 → 把 token/结论导出成**项目内可复用文件**：
   - `tokens.css` / `tokens.scss`（CSS Variables，前端直接引用）
   - 或 `design-tokens.json`（机器可读，供代码/其他工具消费）
   - 或一份 `DESIGN.md`（可编辑、可进项目、可跨工具迁移）
   - 落盘后，报告里注明"已生成 <路径>"。

### 产出质量自检清单（定稿前逐项打勾，缺项补问/补齐）
- [ ] 所有颜色 **像素采样** 给出精确 `#HEX`（无估值）。
- [ ] 字体字号/字重/行高已成表。
- [ ] 文字**超长**的每处都问过：换行/单行省略/几行 clamp？单行省略是否加 `title`/`Tooltip`？
- [ ] 图片**每类**都问过：固定宽高 vs 宽高比(几比几)、`object-fit`、头像是否圆形裁切？
- [ ] 元素**数量可能多**处都问过滚动：容器内滚动 vs 随内容延伸，并**告知影响**（是否整页滚/多滚动条/性能）？
- [ ] **表格**（如有）都问过：每列列宽、是否固定列、是否接受横向滚动条？
- [ ] **响应式**：明确定用 rem / vw·vh / clamp，且说明是否设 min/max、视口范围([C])。
- [ ] **断点覆盖**：多视口时给出各断点布局变化，或说明"仅桌面 + 低于宽度会怎样"。
- [ ] 组件树已拆出**可复用原子件**与**页面级私有**。
- [ ] 每个关键组件列出**状态机**（hover/focus/disabled/loading/空/错误/选中）。
- [ ] 未越权：未圈定的板块明确标「略过」，未替用户拍板。
- [ ] 待确认/留白项已单独列出，供后续继续补问。

**定稿时自查**：是否所有重要结论都**来自用户回答**，而非我默认假设？若有遗漏关键信息，宁可再问一次，不要猜。**尤其文字超长、图片尺寸、"元素数量可能多"时的滚动策略（容器内 vs 随内容延伸、是否整页滚动、是否多滚动条）、表格列宽/固定列、响应式单位与 min/max、视口范围([C])，必须来自用户确认并已告知影响，禁止默认拍板。**

---

## 可复用的决策矩阵速查（随 Skill 附带）

```
0. 工作方式  提问驱动：先问→定向深挖→边问边定→汇总。一次问1-3个，带推荐选项。
1. 类型？    后台 → flex/grid + px/rem, 不用vw; 动效克制
             C端  → vw/vh + clamp(), 可大胆动效
          ⚠️ 类型只是背景参考，不直接决定单位。
[A] 响应式？  整页流式(vw/vh) → 单位用 vw/vh 或 rem 等比; 但正文文字 rem/clamp 兜底
             固定宽高+弹性 → flex/grid + px/rem, 不用 vw 做文字
             混合 → 主体弹性, 全屏 hero 区块用 vw/vh
[B] 宽高？    只最小 / 只最大 / 都设 / 都不设 → 决定 min-width 与 max-width, 勿默认
[C] 视口？    只有桌面 / 多视口(桌面+平板+移动) → 多视口给出各断点布局变化
              通用断点: sm640 md768 lg1024 xl1280; 后台常从1280起, 低于折叠
2. 单位？    文字/间距 → rem; 布局 → flex/grid; 细线/图标 → px; 流式文字 → clamp()
             不用 vw 做正文（除非用户选整页流式，也要 rem/clamp 兜底）
3. 布局？    单轴 → Flex; 多列独立区 → Grid
4. 宽度？    min-width(侧栏/每列/页面); 可选 max-width(超宽防拉长)
5. 滚动？    元素可能多 → 【必问】容器内滚动 vs 随内容延伸, 并告知影响
             容器内 → html/body 100vh; 各列 min-height:0; overflow-y:auto; 整页不滚
             延伸   → body 被撑高整页滚; header 用 sticky
             ⚠️ 告知: 容器内滚动可能出现"外层+内层"多滚动条; 长内容用虚拟滚动
             ⚠️ 不同区域可不同策略, 不要一刀切
6. 超长？    判断有超长风险 → 【必问】处理方式(换行/单行省略/几行clamp/是否tooltip)
             方案: short→单行省略(+可tooltip); long title→2行clamp; pills排不下→横滚
             ⚠️ 单行省略必须问用户是否加 title/Tooltip 展示全文
7. 图片？    出现图片/缩略图/头像 → 【必问】固定宽高 vs 保持宽高比(几比几)
             方案: 封面→aspect-ratio 16/9+cover; 头像→固定+cover+圆形裁切
             ⚠️ 必须问 object-fit(cover/contain/fill) 与是否圆形裁切
8. 表格？    涉及表格 → 【必问】每列列宽(固定/弹性/等宽/混合) + 是否固定列(哪列)
             方案: 文本列弹性+min-width; 数字列固定+右对齐; 操作列固定+sticky right
             ⚠️ 告知: 固定列+横向滚动会出现横向滚动条; 列宽弹性随内容变
9. 状态？    每组件补 default/hover/focus/disabled/loading/空/错误/选中
10. token？  全部像素采样给精确 #HEX; 配色/字体/圆角/阴影/间距/z-index 一表
11. 产物？   可选落盘: tokens.css / tokens.scss / design-tokens.json / DESIGN.md
             ⚠️ 不只是会话文本, 而是项目内可复用文件
12. 自检？   定稿前过一遍质量 checklist(token、超长、图片、滚动、表格、响应式、断点、组件树、状态机) 逐项打勾
13. 取舍？   用户圈定的深挖, 其余「略过」; 不确定的补问, 不替用户假设
```
